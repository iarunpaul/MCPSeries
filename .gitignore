# --- Additions for modern .NET apps -----------------------------------------

# JetBrains Rider / IntelliJ
.idea/
*.sln.iml
.vs/
*.vscode/
.vscode/*
[Bb]in/
[Oo]bj/

# OS cruft
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# ASP.NET Core local config (keep canonical env files tracked)
appsettings.Local.json
appsettings.*.Local.json
appsettings.*.local.json
appsettings.Development.local.json
# Keep the standard ones
!appsettings.json
!appsettings.Development.json
!appsettings.Staging.json
!appsettings.Production.json
# VS launch profiles (optional — uncomment to ignore locally)
# Properties/launchSettings.json

# Azure Functions (local only)
local.settings.json
.azurefunctions/

# Environment files
.env
.env.*
*.env
!.env.example
!.env.sample
!.env.template

# Sonar (scanner & analysis)
.sonarqube/
.scannerwork/

# Docker local overrides
docker-compose.*.override.yml

# SPA / Tooling (Angular + Nx + Storybook commonly paired with ASP.NET Core)
dist/
coverage/
.angular/
.angular/cache/
.nx/cache/
.eslintcache
storybook-static/

# End-to-end test runners (optional)
playwright-report/
blob-report/
test-results/
playwright/.cache/
cypress/videos/
cypress/screenshots/

# Visual Studio service artifacts
ServiceDependencies/
ServiceReferences/
